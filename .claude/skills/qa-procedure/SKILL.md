---
name: qa-procedure
description: "Run the repository's QA procedures (QA-1..QA-7) as executable steps with preconditions, evidence requirements and STOP points, rather than as a checklist someone is expected to recall. (Mode: per procedure — see the table)"
---
<!-- generated by scripts/sync_agentic_adapters.py — do not edit -->
# qa-procedure

**Adapter surface**: `claude`
**Canonical source**: `agentic/skills/qa-procedure/SKILL.md`
**Authority**: `AGENTS.md` + `agentic/manifest.yaml`

Read the canonical source in full before acting. This file is a thin pointer
and deliberately contains no policy text — duplicating it here would create a
second copy that can disagree with the first.

The AUTO / CONSULT / STOP protocol in `AGENTS.md` binds on this surface exactly
as it does on every other.

To change this, edit `agentic/skills/qa-procedure/SKILL.md`, then run:

```bash
python scripts/sync_agentic_adapters.py
python scripts/validate_agentic_surface.py --strict
```
