[project]
name = "data-contracts"
version = "0.1.0"
description = "See libs/data-contracts/src/data_contracts/__init__.py"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/data_contracts"]
