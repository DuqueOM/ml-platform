"""Schema contracts and their evolution rules.

Pandera models at the code boundary; validation suites at the warehouse
boundary. Owns the contract, never the storage or the orchestration.
"""

__version__ = "0.1.0"
