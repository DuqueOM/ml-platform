[project]
name = "serving-core"
version = "0.1.0"
description = "See libs/serving-core/src/serving_core/__init__.py"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/serving_core"]
