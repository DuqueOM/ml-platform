# Demand Forecast

**Kind**: tabular · **Owner**: platform-team · **Dataset**: `nyc-tlc`

> Generated by this repository's project generator (`copier.yml`). Structure is
> uniform across every project by design — that uniformity is what lets one
> agentic skill operate on all of them (ADR-001).

## What it predicts, for whom, at what cost of error

<!-- REQUIRED. Not a formality: the cost of a false positive versus a false
     negative is what sets the decision threshold in evals/gates.yaml. A
     threshold with no recorded reason gets lowered by whoever it first
     blocks. -->

TODO — state the prediction target, the consumer, and the asymmetry between
error types in the units the business actually uses.

## Layout

```
contracts/   schema contracts (Pandera at the code boundary)
evals/       quality gates AS DATA — thresholds with their rationale
src/demand_forecast/
tests/       unit, contract, behavioural
docs/decisions/   ADRs whose blast radius is THIS PROJECT only
model-card.md     intended use, limits, fairness, failure modes
```

An ADR whose consequences reach beyond this project belongs in the repository
root `docs/decisions/`, not here. The split is by blast radius.

## Serving

The serving component is **generated** from
[`ml-service-template`](https://github.com/DuqueOM/ml-service-template) via
copier (ADR-003), never hand-written. Where this project and the template
disagree about serving, containers, probes or supply chain, the template is
correct.

## Gates

Run what CI runs:

```bash
uv run pytest projects/demand-forecast -q
uv run python -m demand_forecast.gates --check
```

Every published quality claim about this project must map to a row in
`docs/governance/quality-gates.md`. A metric that cannot fail a build is
decoration.
