# Runtime dependencies. ML-style compatible-release pinning (~=) per project
# convention: avoids silent breakage from majors while allowing patch/minor.
fastapi~=0.115
uvicorn~=0.34
httpx~=0.28
pydantic~=2.10
rank-bm25~=0.2.2
pyyaml~=6.0
