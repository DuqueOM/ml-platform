# This file is AUTOGENERATED by Copier — do not edit by hand.
#
# It records the template source, the template version (`_commit`), and every
# answer used to generate this service. `copier update` reads it to replay the
# generation against a newer template revision and three-way-merge the result
# into your working tree.
#
# Without this file there is no update path, and the generated service is a
# fork with extra steps (ADR-003 "Generation, not copying"). It is therefore a
# REQUIRED deliverable of the template, not an optional convenience — the
# `scaffold-update` workflow and `_message_after_copy` both depend on it.
#
# Keep it committed. Deleting it strands the service on the template revision
# it was born from.
_commit: v0.26.0
_src_path: /home/duqueom/projects/main_projects/template_MLOps
gh_org: DuqueOM
gh_repo: ml-platform
profile: local
service_slug: demand_forecast_serving

