# Bootstrap uses local Terraform state — these files contain resource IDs
# but should NOT be committed (per-operator metadata, conflicts on multi-op
# applies). Operators back up state to a private location after each apply.
terraform.tfstate
terraform.tfstate.backup
terraform.tfstate.*
.terraform/
.terraform.lock.hcl
crash.log
crash.*.log
