apiVersion: v1
kind: ServiceAccount
metadata:
  name: "demand-forecast-serving-sa"
  labels:
    app: "demand-forecast-serving"
  annotations:
    # GCP Workload Identity (uncomment for GKE):
    # iam.gke.io/gcp-service-account: demand-forecast-serving-sa@{PROJECT_ID}.iam.gserviceaccount.com

    # AWS IRSA (uncomment for EKS):
    # eks.amazonaws.com/role-arn: arn:aws:iam::{ACCOUNT_ID}:role/demand-forecast-serving-role
