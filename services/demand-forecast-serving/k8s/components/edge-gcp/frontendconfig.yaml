# Enforces the ADR-042 TLS floor at the load balancer: minimum TLS 1.2,
# HTTP requests redirected to HTTPS rather than served in plaintext.
apiVersion: networking.gke.io/v1beta1
kind: FrontendConfig
metadata:
  name: "demand-forecast-serving-frontend-config"
spec:
  sslPolicy: "demand-forecast-serving-ssl-policy"
  redirectToHttps:
    enabled: true
    responseCodeName: MOVED_PERMANENTLY_DEFAULT
