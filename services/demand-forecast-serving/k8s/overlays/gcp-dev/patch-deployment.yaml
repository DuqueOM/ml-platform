# =============================================================================
# GCP Dev — Deployment patches
# =============================================================================
# 1 replica, minimum resources to keep dev cluster cheap.
# =============================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: "demand-forecast-serving-predictor"
spec:
  replicas: 1
  template:
    spec:
      initContainers:
        - name: model-downloader
          # Image is set by the overlay's `images:` mapping in
          # kustomization.yaml (PR-R2-2). Setting `image:` here would
          # bypass digest pinning enforced by the CI lint.
          command:
            - gsutil
            - cp
            - "gs://{MODEL_BUCKET}/demand-forecast-serving/model.joblib"
            - /models/model.joblib
          volumeMounts:
            - name: model-storage
              mountPath: /models
      containers:
        - name: "demand-forecast-serving-predictor"
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
          env:
            - name: MODEL_PATH
              value: /models/model.joblib
            - name: LOG_FORMAT
              value: "json"
            - name: LOG_LEVEL
              value: "DEBUG"
            - name: ENVIRONMENT
              value: "dev"
