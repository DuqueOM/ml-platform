# =============================================================================
# GCP Dev — ServiceAccount with Workload Identity
# =============================================================================
apiVersion: v1
kind: ServiceAccount
metadata:
  name: "demand-forecast-serving-sa"
  annotations:
    iam.gke.io/gcp-service-account: "demand-forecast-serving-sa-dev@{PROJECT_ID}.iam.gserviceaccount.com"
