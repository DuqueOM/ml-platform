# =============================================================================
# GCP Staging — ServiceAccount with Workload Identity
# =============================================================================
# Same WI binding as prod; the staging IAM SA should have read-only access
# to staging GCS buckets only.
# =============================================================================
apiVersion: v1
kind: ServiceAccount
metadata:
  name: "demand-forecast-serving-sa"
  annotations:
    iam.gke.io/gcp-service-account: "demand-forecast-serving-sa-staging@{PROJECT_ID}.iam.gserviceaccount.com"
